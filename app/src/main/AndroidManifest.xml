<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.covidtoolkit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.CovidToolkit"/>
        <activity android:name=".VaccineInformationActivity" />
        <activity android:name=".ReqionalQuarantineInformationActivity" />
        <activity android:name=".HowToQuarantineActivity" />
        <activity android:name=".QuarantineGeneralInformation" />
        <activity android:name=".QuarantineInformation" />
        <activity
            android:name=".CovidScreeningActivity" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>